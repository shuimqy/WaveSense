<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme - iOS 蓝色风格 -->
    <style name="Base.Theme.WaveSense" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryContainer">@color/primary_bg</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/heart_rate</item>
        <item name="colorSurface">@color/bg_card</item>
        <item name="android:colorBackground">@color/bg_page</item>
    </style>

    <style name="Theme.WaveSense" parent="Base.Theme.WaveSense" />

    <!-- Profile 页面菜单项样式 -->
    <style name="ProfileMenuItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:drawablePadding">16dp</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">16sp</item>
        <item name="android:drawableTint">@color/primary</item>
    </style>

    <!-- 卡片样式 -->
    <style name="CardStyle">
        <item name="android:background">@drawable/bg_card_white</item>
        <item name="android:padding">16dp</item>
    </style>

    <!-- 状态徽章文字样式 -->
    <style name="StatusBadge">
        <item name="android:textSize">11sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:paddingTop">3dp</item>
        <item name="android:paddingBottom">3dp</item>
    </style>

    <!-- 章节标题样式 -->
    <style name="SectionTitle">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:layout_marginBottom">12dp</item>
    </style>
</resources>
