<?xml version="1.0" encoding="utf-8"?>
<!-- 个人资料页 - 对齐 iOS SettingsView：蓝色渐变头部 + 白色卡片 + 菜单列表 -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_surface"
    android:fillViewport="true"
    tools:context=".app.ui.fragment.ProfileFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="40dp">

        <!-- 头部蓝色渐变背景 -->
        <View
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:background="@drawable/bg_profile_header" />

        <!-- 用户信息卡片（负 margin 浮于头部之上） -->
        <androidx.cardview.widget.CardView
            android:id="@+id/userCard"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="20dp"
            android:layout_marginTop="-80dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="8dp">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="20dp">

                <!-- 头像：圆形 + 渐变背景 -->
                <FrameLayout
                    android:id="@+id/ivAvatarFrame"
                    android:layout_width="70dp"
                    android:layout_height="70dp"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent">

                    <View
                        android:layout_width="70dp"
                        android:layout_height="70dp"
                        android:background="@drawable/bg_logo_circle" />

                    <ImageView
                        android:id="@+id/ivAvatar"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:padding="10dp"
                        android:src="@mipmap/ic_launcher_round" />

                </FrameLayout>

                <!-- 用户名 -->
                <TextView
                    android:id="@+id/tvUsername"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="16dp"
                    android:layout_marginTop="4dp"
                    android:text="加载中..."
                    android:textColor="@color/text_primary"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    app:layout_constraintStart_toEndOf="@id/ivAvatarFrame"
                    app:layout_constraintTop_toTopOf="@id/ivAvatarFrame" />

                <!-- 角色徽章 -->
                <TextView
                    android:id="@+id/tvRole"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:background="@drawable/bg_role_badge"
                    android:paddingHorizontal="10dp"
                    android:paddingVertical="4dp"
                    android:text="身份: 家属"
                    android:textColor="@color/primary"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    app:layout_constraintStart_toStartOf="@id/tvUsername"
                    app:layout_constraintTop_toBottomOf="@id/tvUsername" />

            </androidx.constraintlayout.widget.ConstraintLayout>

        </androidx.cardview.widget.CardView>

        <!-- 菜单列表区域 -->
        <androidx.cardview.widget.CardView
            android:id="@+id/menuCard"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="20dp"
            android:layout_marginTop="16dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="3dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/btnMyDevice"
                    style="@style/ProfileMenuItem"
                    android:drawableStart="@android:drawable/ic_menu_rotate"
                    android:text="我的设备 (雷达/手环)" />

                <View android:layout_width="match_parent" android:layout_height="1dp" android:background="@color/divider" />

                <TextView
                    android:id="@+id/btnAlarmPreference"
                    style="@style/ProfileMenuItem"
                    android:drawableStart="@android:drawable/ic_menu_preferences"
                    android:text="报警偏好设置" />

                <View android:layout_width="match_parent" android:layout_height="1dp" android:background="@color/divider" />

                <TextView
                    android:id="@+id/btnChangePwd"
                    style="@style/ProfileMenuItem"
                    android:drawableStart="@android:drawable/ic_lock_lock"
                    android:text="修改密码" />

                <View android:layout_width="match_parent" android:layout_height="1dp" android:background="@color/divider" />

                <TextView
                    android:id="@+id/btnAbout"
                    style="@style/ProfileMenuItem"
                    android:drawableStart="@android:drawable/ic_menu_info_details"
                    android:text="关于 WaveSense" />

            </LinearLayout>

        </androidx.cardview.widget.CardView>

        <!-- 退出登录按钮 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnLogout"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:layout_marginHorizontal="20dp"
            android:layout_marginTop="24dp"
            android:text="退出登录"
            android:textSize="16sp"
            android:textColor="@color/white"
            android:textStyle="bold"
            app:backgroundTint="@color/logout_red"
            app:cornerRadius="28dp"
            app:elevation="3dp" />

    </LinearLayout>

</ScrollView>
